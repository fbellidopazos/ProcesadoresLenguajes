/* Ejemplo de number*/

let string cadena;input(cadena);
let boolean logico1;let boolean logico2;
let number number2;
number1 = 378;

number2 = number1;
cadena = "string";

function string ff(string ss)
{
	logico2 =  logico1;
	if (logico2) cadena = ff (ss);
	varglobal = 78;
	return cadena;
}
		
function string funcion (string logico2)
{	let number var;

  do{
	logico1 = number1 == number2;alert(9876);logico2="";
  }while(logico1);

  return logico2;
}

alert(cadena);
cadena = ((ff(funcion(cadena))));

